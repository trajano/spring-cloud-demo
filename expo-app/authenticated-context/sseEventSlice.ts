export {};
// import { createSlice } from "@reduxjs/toolkit";
// import type { PayloadAction } from "@reduxjs/toolkit";

// const initialState: string[] = [];
// export const sseEventSlice = createSlice({
//   name: "sseEvent",
//   initialState,
//   reducers: {
//     receivedEvent(state, action: PayloadAction<string>) {
//       state.push(action.payload);
//       state.splice(0, Math.max(state.length - 5, 0));
//     },
//   },
// });
// export const { receivedEvent } = sseEventSlice.actions;
